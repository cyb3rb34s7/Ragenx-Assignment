Hi Raja,

 

Thank you for your interest in the role and for the conversations so far. We would like to move forward with a technical exercise to understand how you build.

 

The exercise is in two phases:

 

    A take-home (about 2.5 hours of focused work) that you complete in the 48 hours before our live session. It has two parts: a Java + Spring Boot backend (about 90 minutes) and the operational tooling around it including Docker file, shell scripts, and a runbook (about 60 minutes). You can use Claude Code throughout.
    A 45-minute live React build over screen share, followed by a 10-minute walkthrough. You will build a frontend that calls the backend you just shipped.

 

Please use Claude Code as your AI tool of choice. We are specifically interested in seeing how you work with it.

 

The full brief is attached as `BUILD_EXERCISE_BRIEF.md`. Please read it end to end before you start. The bootstrap data file `case_v1.json` is also attached. A second data file for the live session will be shared at the start of the call.

 

A few logistics:

 

    Please create one public GitHub repo for everything (folders `/backend`, `/ops`, `/frontend`) and share your GitHub username with us so we can be added as a collaborator.
    Commit 10-15 mins in all phases. We would like to see the progression, not just the final state.
    Push Phase 1A and Phase 1B before the live session. Have the service running locally on a known port when we start.
    After the live session, please save your Claude Code session log (located at `~/.claude/projects/<encoded-project-path>/<session-id>.jsonl` on macOS or Linux, `%USERPROFILE%\.claude\projects\...` on Windows) and either commit it to the repo at `/claude-code-session.jsonl` or share it via email. We use it as part of the evaluation.

 

Let us know if you have any questions on the brief. 

 

Looking forward to it.